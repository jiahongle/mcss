import React from 'react';
import Banner from '../banner/banner.js';


export default class upcomingEvents extends React.Component {

    render() {

        return (
            <Banner name={'Current events'} />
        )
    }
}